# 🚀 دليل الانتقال إلى السيرفر الحي (Production Deployment Guide)

## 🌟 تحديث عاجل (2026-01-24): إصلاح فلترة المحطات (Station Filtering Fix)

لحل مشكلة عدم تغير البيانات عند تغيير المحطة، واتاحة "Global View" بشكل صحيح.

### الخطوات:

1.  **حمل الملف**: قم بتنزيل ملف `deployment_update.zip` الذي أنشأته لك.
2.  **ارفع الملف**: قم برفع هذا الملف إلى المجلد الرئيسي للمشروع في السيرفر (بجانب مجلد `app` و `public`).
3.  **فك الضغط**: قم بفك الضغط واختيار **استبدال الكل (Overwrite All)**.

---

## 1. إعداد قاعدة البيانات (Database)

- لقد قمنا بتحديث ملف الاتصال ليعتمد على ملف إعدادات خارجي.
- **في السيرفر الحي**:
  1. اذهب إلى المجلد `app/Config/`.
  2. أنشئ ملفاً جديداً باسم `db_config.php`.
  3. ضع فيه بيانات الاتصال الخاصة بالسيرفر الحي كما يلي:
  ```php
  <?php
  return [
      'host' => 'localhost', // غالباً لا تتغير
      'db_name' => 'اسم_قاعدة_البيانات_الحقيقية',
      'username' => 'اسم_مستخدم_قاعدة_البيانات',
      'password' => 'كلمة_المرور_القوية',
  ];
  ```

## 2. تفعيل التشفير (SSL / HTTPS)

- ملف `.htaccess` في مجلد `public` جاهز الآن.
- بعد رفع الموقع، افتح الملف `public/.htaccess` وقم بإزالة علامة التعليق (`#`) عن السطور التالية:
  ```apache
  # RewriteCond %{HTTPS} off
  # RewriteRule ^(.*)$ https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301]
  ```
  (اجعلها تبدو كالتالي):
  ```apache
  RewriteCond %{HTTPS} off
  RewriteRule ^(.*)$ https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301]
  ```

## 3. بناء واجهة المستخدم (Build Frontend)

- تأكد دائماً من تشغيل الأمر التالي قبل رفع الملفات لضمان ظهور آخر التعديلات:
  ```bash
  npm run build
  ```
- هذا سيقوم بتحديث الملفات داخل `public/build`.

## 4. الروابط (URLs)

- النظام الآن يكتشف الدومين تلقائياً (`BASE_URL` في `app/Config/Constants.php`).
- إذا كان الدومين هو `https://app.petrodiesel.net`، سيعمل النظام مباشرة دون تعديل.

## قائمة التحقق (Checklist)

- [x] رفع الملفات إلى السيرفر.
- [x] إنشاء ملف `app/Config/db_config.php` بالبيانات الصحيحة.
- [x] استيراد قاعدة البيانات (SQL) إلى السيرفر.
- [x] تفعيل HTTPS في `.htaccess`.
- [x] التأكد من صلاحيات المجلدات (خاصة مجلدات `build` و `logs` إذا وجدت).
